@model IEnumerable<TestAppMVC.ServiceReference1.GameTable>

@{
    ViewBag.Title = "GameDetails";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>GameDetails</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PlayerA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PlayerB)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Result)
        </th>
        @*<th></th>*@
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PlayerA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PlayerB)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Result)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>*@
    </tr>
}

</table>
